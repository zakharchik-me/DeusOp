version: "3.8"

services:
  nginx:
    build: .
    container_name: mynginx
    ports:
      - "8080:80"
    volumes:
      - "/home/daniil/Documents/ITMO/devOps/static:/var/www/html:ro"
      - "./nginx.conf:/etc/nginx/nginx.conf:ro"
    restart: unless-stopped
